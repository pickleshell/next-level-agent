# Next Level Agent Release Notes

## v0.1.0-alpha.2 (2026-09-02)

- Establishes [`nla-version.json`](nla-version.json) as the canonical NLA
  release identity and records the exact historical source-base commit.
- Keeps NLA versioning independent from inherited source metadata.
- Adds a deterministic consistency check for version and tag derivation.

## v0.1.0-alpha.1 (2026-09-02)

First publication-focused experimental Alpha:

- bounded Compactor tool-schema shortlisting with cached role capabilities;
- native Ollama and generic OpenAI-compatible utility runtime;
- controlled checkpoint, native compaction, and restoration;
- sanitized local and cloud measurement evidence;
- deterministic CI and healthy public model defaults.

NLA is experimental OpenCode Alpha software, not a hardened security boundary.
